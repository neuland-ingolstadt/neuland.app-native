overwrite: true
schema: 'https://api.dev.neuland.app/graphql'
documents: 'src/api/**/*.ts'
generates:
    'src/gql/':
        preset: 'client'
        presetConfig:
            fragmentMasking:
                unmaskFunctionName: 'getFragmentData'

config:
    scalars:
        DateTime: 'Date'
        URL: 'string'
        LocalEndTime: 'string'
        EmailAddress: 'string'
