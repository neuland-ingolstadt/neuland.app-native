import { Stack } from "expo-router";

export default function TimetableScreen() {
  return (
    <>
      <Stack.Screen
        options={{
          title: "Timetable",
        }}
      />
    </>
  );
}
