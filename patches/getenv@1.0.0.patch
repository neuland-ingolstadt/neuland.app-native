diff --git a/index.js b/index.js
index 5e83c8f8ce74cade53466fc82bd6809358762a58..72eb7065f3eb723f41b9fb9c496d8c8c25731fe8 100644
--- a/index.js
+++ b/index.js
@@ -54,18 +54,18 @@ const convert = {
     return +value;
   },
   bool: function(value) {
-    const isBool = value === 'true' || value === 'false';
+    const isBool = value === 'true' || value === 'false' || value === 'TRUE' || value === 'FALSE';
     if (!isBool) {
       throw new Error('GetEnv.NoBoolean: ' + value + ' is not a boolean.');
     }
 
-    return value === 'true';
+    return value === 'true' || value === 'TRUE';
   },
   boolish: function(value) {
     try {
       return convert.bool(value);
     } catch (err) {
-      const isBool = value === '1' || value === '0';
+      const isBool = value === 'true' || value === 'false' || value === 'TRUE' || value === 'FALSE';
       if (!isBool) {
         throw new Error('GetEnv.NoBoolean: ' + value + ' is not a boolean.');
       }
